# 内容哈希缓存 实现计划(F3D 落地项 4/4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/superpowers/specs/2026-08-19-ibl-cache-design.md` 实现 IBL 预滤波内容哈希磁盘缓存。

**关键事实(代码侦察)**:
- Environment::build 预滤波直接渲染进 cube face/mip(`colorFromTexture`);
  BRDF LUT 是 CPU 纯函数(`integrateBrdfLut(kLutSize)`,常量输入)**不缓存**;
  环境源是程序生成的 env_.faces(CPU 像素,天然可哈希)。
- RHI 已备:`updateTexture(tex, mip, face, data, size)`(cube 子资源上传)+
  `readbackTexture(target, out, size)`(目标读回 RGBA8)。
- 缓存开启时:逐 face/mip 渲到 RGBA8 离屏目标 → readback → 写缓存 + updateTexture 上传;
  命中 → 跳过渲染直接 updateTexture。

**通用约定:**
- 构建+测试:`export RD_DEPS_MIRROR=$HOME/.rd_deps_mirror && ./scripts/check.sh`
- 提交规范:每 Task 一个 commit,`<type>(<scope>): 描述`

---

### Task 1: hash + 缓存文件 IO + 单测

**Files:**
- Create: `core/foundation/hash.h`(FNV-1a 64)、`core/resource/ibl_cache.{h,cpp}`
- Modify: `core/CMakeLists.txt`
- Test: `tests/resource/ibl_cache_test.cpp`(新建)、`tests/CMakeLists.txt`(注册)

- [ ] **Step 1: 失败测试**

`tests/resource/ibl_cache_test.cpp`:

```cpp
// IBL 缓存单测:hash 确定性/雪崩;文件写读往返;坏文件拒绝。
#include <gtest/gtest.h>
#include <cstdio>
#include <filesystem>
#include "foundation/hash.h"
#include "resource/ibl_cache.h"

namespace fs = std::filesystem;

TEST(Hash, DeterministicAndAvalanche) {
  const uint8_t a[4] = {1, 2, 3, 4}, b[4] = {1, 2, 3, 5};
  EXPECT_EQ(rd::fnv1a64(a, 4), rd::fnv1a64(a, 4));   // 确定性
  EXPECT_NE(rd::fnv1a64(a, 4), rd::fnv1a64(b, 4));   // 雪崩
  EXPECT_NE(rd::fnv1a64(a, 4), rd::fnv1a64(a, 3));   // 长度敏感
}

TEST(IblCache, RoundTrip) {
  const fs::path dir = fs::temp_directory_path() / "rd_iblcache_test";
  fs::remove_all(dir);
  rd::IblCacheBlob blob;
  blob.size = 4; blob.mips = 3;
  // face×mip 像素(mip 尺寸 4/2/1,每面 4*4*4 + 2*2*4 + 1*1*4 B)
  for (uint32_t m = 0; m < blob.mips; ++m) {
    const uint32_t s = blob.size >> m;
    for (uint32_t f = 0; f < 6; ++f) {
      std::vector<uint8_t> px(size_t(s) * s * 4);
      for (size_t i = 0; i < px.size(); ++i) px[i] = uint8_t((i + f * 7 + m * 13) & 0xFF);
      blob.faces.push_back(std::move(px));
    }
  }
  ASSERT_TRUE(rd::iblCacheWrite(dir.string(), 0xDEADBEEF, blob));
  rd::IblCacheBlob got;
  ASSERT_TRUE(rd::iblCacheRead(dir.string(), 0xDEADBEEF, got));
  EXPECT_EQ(got.size, blob.size);
  EXPECT_EQ(got.mips, blob.mips);
  ASSERT_EQ(got.faces.size(), blob.faces.size());
  for (size_t i = 0; i < blob.faces.size(); ++i)
    EXPECT_EQ(got.faces[i], blob.faces[i]);
  // 未命中
  rd::IblCacheBlob miss;
  EXPECT_FALSE(rd::iblCacheRead(dir.string(), 0x1234, miss));
  fs::remove_all(dir);
}

TEST(IblCache, RejectCorrupt) {
  const fs::path dir = fs::temp_directory_path() / "rd_iblcache_test2";
  fs::remove_all(dir);
  fs::create_directories(dir / "ibl");
  // 写坏文件(magic 错)
  const fs::path p = dir / "ibl" / "0000000000000001.ibc";
  FILE* f = fopen(p.string().c_str(), "wb");
  ASSERT_NE(f, nullptr);
  const char junk[8] = {'X','X','X','X','X','X','X','X'};
  fwrite(junk, 1, 8, f);
  fclose(f);
  rd::IblCacheBlob got;
  EXPECT_FALSE(rd::iblCacheRead(dir.string(), 1, got));
  fs::remove_all(dir);
}
```

`tests/CMakeLists.txt` 追加 `resource/ibl_cache_test.cpp`。

- [ ] **Step 2: 跑测试确认失败**

Run: `./scripts/check.sh 2>&1 | tail -5`
Expected: 编译错误(`foundation/hash.h` 不存在)

- [ ] **Step 3: 实现**

`core/foundation/hash.h`:

```cpp
/**
 * @file hash.h
 * @brief FNV-1a 64 位内容哈希(缓存键/内容寻址)。
 */
#pragma once
#include <cstdint>
#include <cstring>

namespace rd {

inline uint64_t fnv1a64(const void* data, size_t size, uint64_t h = 14695981039346656037ull) {
  const auto* p = static_cast<const uint8_t*>(data);
  for (size_t i = 0; i < size; ++i) {
    h ^= p[i];
    h *= 1099511628211ull;
  }
  return h;
}
/// 链式混入标量(尺寸/参数)。
inline uint64_t fnv1a64(uint64_t v, uint64_t h) { return fnv1a64(&v, sizeof(v), h); }

} // namespace rd
```

`core/resource/ibl_cache.h`:

```cpp
/**
 * @file ibl_cache.h
 * @brief IBL 预滤波磁盘缓存:内容哈希键 → 逐 face/mip RGBA8 像素 blob。
 * 原子写(临时文件+rename);坏文件(magic/version/尺寸不符)拒绝。
 */
#pragma once
#include <cstdint>
#include <string>
#include <vector>

namespace rd {

/// 预滤波 cube 像素 blob:faces 按 mip-major(face×mip 顺序:faces[mip*6+face])。
struct IblCacheBlob {
  uint32_t size = 0;   ///< cube 边长(mip0)
  uint32_t mips = 0;   ///< mip 级数
  std::vector<std::vector<uint8_t>> faces;  ///< faces[mip*6+face],RGBA8 紧凑
};

/// 写缓存(目录不存在自动创建 ibl/ 子目录;原子 rename)。成功 true。
bool iblCacheWrite(const std::string& cacheDir, uint64_t key, const IblCacheBlob& blob);
/// 读缓存;未命中/坏文件 false。
bool iblCacheRead(const std::string& cacheDir, uint64_t key, IblCacheBlob& out);

} // namespace rd
```

`core/resource/ibl_cache.cpp`:

```cpp
#include "resource/ibl_cache.h"
#include "foundation/log.h"
#include <cstdio>
#include <filesystem>

namespace rd {
namespace fs = std::filesystem;

namespace {
constexpr uint32_t kMagic = 0x52444249;  // 'RDBI'
constexpr uint32_t kVersion = 1;

std::string pathFor(const std::string& dir, uint64_t key) {
  char name[32];
  snprintf(name, sizeof(name), "%016llx.ibc", (unsigned long long)key);
  return dir + "/ibl/" + name;
}
} // namespace

bool iblCacheWrite(const std::string& cacheDir, uint64_t key, const IblCacheBlob& blob) {
  if (cacheDir.empty() || blob.faces.empty()) return false;
  std::error_code ec;
  fs::create_directories(cacheDir + "/ibl", ec);
  if (ec) return false;
  const std::string tmp = pathFor(cacheDir, key) + ".tmp";
  FILE* f = fopen(tmp.c_str(), "wb");
  if (!f) return false;
  bool ok = true;
  const uint32_t hdr[4] = {kMagic, kVersion, blob.size, blob.mips};
  ok &= fwrite(hdr, 4, 4, f) == 4;
  for (const auto& px : blob.faces) {
    const uint32_t n = uint32_t(px.size());
    ok &= fwrite(&n, 4, 1, f) == 1;
    ok &= fwrite(px.data(), 1, n, f) == n;
  }
  fclose(f);
  if (!ok) { fs::remove(tmp, ec); return false; }
  fs::rename(tmp, pathFor(cacheDir, key), ec);
  return !ec;
}

bool iblCacheRead(const std::string& cacheDir, uint64_t key, IblCacheBlob& out) {
  if (cacheDir.empty()) return false;
  FILE* f = fopen(pathFor(cacheDir, key).c_str(), "rb");
  if (!f) return false;
  uint32_t hdr[4];
  if (fread(hdr, 4, 4, f) != 4 || hdr[0] != kMagic || hdr[1] != kVersion) {
    fclose(f);
    return false;
  }
  IblCacheBlob b;
  b.size = hdr[2];
  b.mips = hdr[3];
  if (b.size == 0 || b.mips == 0 || b.size > 4096 || b.mips > 12) {
    fclose(f);
    return false;  // 尺寸离谱→坏文件
  }
  const uint32_t nFace = b.mips * 6;
  for (uint32_t i = 0; i < nFace; ++i) {
    uint32_t n = 0;
    if (fread(&n, 4, 1, f) != 1) { fclose(f); return false; }
    // 期望尺寸校验(mip i/6 的边长 ×4B)
    const uint32_t m = i / 6;
    const uint32_t s = std::max(1u, b.size >> m);
    if (n != s * s * 4) { fclose(f); return false; }
    std::vector<uint8_t> px(n);
    if (fread(px.data(), 1, n, f) != n) { fclose(f); return false; }
    b.faces.push_back(std::move(px));
  }
  fclose(f);
  out = std::move(b);
  return true;
}

} // namespace rd
```

`core/CMakeLists.txt` 追加 `resource/ibl_cache.cpp`。

- [ ] **Step 4: 跑测试**

Run: `./scripts/check.sh 2>&1 | tail -5`
Expected: 全绿(Hash.* / IblCache.* 通过)

- [ ] **Step 5: Commit**

```bash
git add core/foundation/hash.h core/resource/ibl_cache.* core/CMakeLists.txt tests/resource/ibl_cache_test.cpp tests/CMakeLists.txt
git commit -m "feat(resource): FNV-1a 哈希 + IBL 预滤波磁盘缓存 IO(原子写/坏文件拒绝)"
```

---

### Task 2: Renderer/Environment 接线 + golden 双跑

**Files:**
- Modify: `core/renderer/environment.{h,cpp}`(cacheDir + 命中跳过/未命中读回上传)
- Modify: `core/renderer/renderer.{h,cpp}`(setCacheDir 透传)
- Modify: `core/api/rd_api.{h,cpp}`(rd_engine_set_cache_dir)
- Test: `tests/api/api_test.cpp`(cache API 冒烟)+ golden 双跑手动验证

- [ ] **Step 1: 失败测试**

`tests/api/api_test.cpp` 追加:

```cpp
// IBL 缓存 C API:设置目录不崩;NULL 关闭
TEST(Api, CacheDir) {
  rd_engine* e = rd_engine_create(RD_BACKEND_METAL);
  ASSERT_NE(e, nullptr);
  rd_engine_set_cache_dir(e, "/tmp/rd_test_cache");
  rd_engine_set_cache_dir(e, nullptr);  // 关闭
  rd_engine_destroy(e);
}
```

- [ ] **Step 2: environment.{h,cpp} 改造**

`environment.h`:
- `bool build(Device&, pfVs, pfFs, entry, colorFormat, cubeSize, prefilterMips)` 追加
  `const char* cacheDir = nullptr`(默认 nullptr=关)。
- 实现签名同步。

`environment.cpp` build 内(第 4 步之后,第 6 步渲染循环替换为):

```cpp
  // 6. 预滤波(缓存开启:命中直接上传;未命中渲到离屏目标读回+写盘+上传)
  const std::string cacheDir = cacheDir_ ? cacheDir_ : "";
  uint64_t key = 0;
  if (!cacheDir.empty()) {
    // 键:env 源像素(6 面)+ env size + cubeSize + mips
    key = 14695981039346656037ull;
    for (const auto& fpx : env_.faces) key = rd::fnv1a64(fpx.data(), fpx.size(), key);
    key = rd::fnv1a64(uint64_t(env_.size), key);
    key = rd::fnv1a64(uint64_t(cubeSize), key);
    key = rd::fnv1a64(uint64_t(prefilterMips), key);
    rd::IblCacheBlob hit;
    if (rd::iblCacheRead(cacheDir, key, hit)) {
      for (uint32_t m = 0; m < prefilterMips; ++m)
        for (uint32_t f = 0; f < 6; ++f)
          dev.updateTexture(prefilterCube_, m, f, hit.faces[m * 6 + f].data(),
                            hit.faces[m * 6 + f].size());
      RD_LOGI("renderer.env", "IBL 缓存命中 key=%016llx", (unsigned long long)key);
      return true;
    }
  }
  rd::IblCacheBlob miss;  // 缓存开启时收集读回像素
  if (!cacheDir.empty()) { miss.size = cubeSize; miss.mips = prefilterMips; }
  for (uint32_t mip = 0; mip < prefilterMips; ++mip) {
    const uint32_t sz = std::max(1u, cubeSize >> mip);
    const float roughness = float(mip) / float(prefilterMips - 1);
    for (uint32_t face = 0; face < 6; ++face) {
      OffscreenTargetDesc td;
      td.width = sz;
      td.height = sz;
      if (!cacheDir.empty()) {
        // 缓存模式:渲到自建 RGBA8 目标(读回后上传 cube)
        td.colorFormat = Format::RGBA8_UNORM;
      } else {
        td.colorFromTexture = prefilterCube_;
        td.face = face;
        td.mipLevel = mip;
      }
      auto target = dev.createOffscreenTarget(td);
      if (!target.valid()) return false;
      ... 渲染同现状(updateBuffer/bindPipeline/bindTexture/draw/submit/waitIdle)...
      if (!cacheDir.empty()) {
        std::vector<uint8_t> px(size_t(sz) * sz * 4);
        if (!dev.readbackTexture(target, px.data(), px.size())) {
          dev.destroyTarget(target);
          return false;
        }
        dev.updateTexture(prefilterCube_, mip, face, px.data(), px.size());
        miss.faces.push_back(std::move(px));
      }
      dev.destroyTarget(target);
    }
  }
  if (!cacheDir.empty() && miss.faces.size() == size_t(prefilterMips) * 6) {
    rd::iblCacheWrite(cacheDir, key, miss);
  }
  return true;
}
```

- 头部 `#include "foundation/hash.h" #include "resource/ibl_cache.h"`。
- `environment.h` 加成员 `const char* cacheDir_` 改为 `std::string cacheDir_;`
  + `void setCacheDir(const char* dir) { cacheDir_ = dir ? dir : ""; }`。

- [ ] **Step 3: renderer + api 接线**

`renderer.h`:
```cpp
  /// IBL 预滤波磁盘缓存目录(空=关,默认关;下次环境重建生效)。
  void setCacheDir(const char* dir) { env_.setCacheDir(dir); }
```

`rd_api.h`:
```c
/// 设置 IBL 预滤波磁盘缓存目录;NULL/空串关闭(默认关)。目录自动创建。
void rd_engine_set_cache_dir(rd_engine* engine, const char* path);
```

`rd_api.cpp`:
```cpp
void rd_engine_set_cache_dir(rd_engine* e, const char* path) {
  if (!e) return;
  e->renderer.setCacheDir(path);
  e->renderDirty = true;
}
```

- [ ] **Step 4: 跑测试 + golden 双跑验证**

Run: `./scripts/check.sh 2>&1 | tail -5`(全绿;默认关零回归)

双跑手动验证(命中路径与直接路径像素一致):

```bash
# 工具验证脚本(不进 ctest;或加为 ctest 双跑用例):
./build/tools/render_test/render_test --out /tmp/ibl_cache_miss.png  # 首跑(写缓存需工具支持 --cache-dir……)
```

   **工具接线**:`tools/render_test/main.cpp` 加 `--cache-dir <path>` 参数 →
   `rd_engine_set_cache_dir`;连跑两次同参数,二跑应秒出(日志 "IBL 缓存命中")+
   两图 SSIM=0 完全一致。注册 ctest 双跑用例 `golden_ibl_cache`(RD_GOLDEN_TEST
   对一 golden,跑两次断言二次日志命中+图一致)——简化:ctest 用例
   `ibl_cache_test` 用 --cache-dir /tmp/rd_ctest_cache 跑两次,第二次与第一次
   diffImage=0(非 SSIM,精确一致——同后端同缓存)。
   写入 tests/golden/golden_test.cpp?**新文件 tests/api/ibl_cache_e2e_test.cpp**?——
   不,这是工具级验证;注册进 tests/CMakeLists.txt 为两条 ctest:

```cmake
add_test(NAME golden_ibl_cache_miss COMMAND render_test --backend metal --cache-dir
         ${CMAKE_BINARY_DIR}/test_ibl_cache --out ${CMAKE_BINARY_DIR}/ibl_miss.png)
add_test(NAME golden_ibl_cache_hit COMMAND render_test --backend metal --cache-dir
         ${CMAKE_BINARY_DIR}/test_ibl_cache --out ${CMAKE_BINARY_DIR}/ibl_hit.png)
set_tests_properties(golden_ibl_cache_hit PROPERTIES DEPENDS golden_ibl_cache_miss)
```

   + 两图 compareImage 精确一致(diffRatio==0)——用 img_check?或 CMake 脚本比字节
   `cmake -E compare_files`。用 `compare_files`(同后端同参数确定性):
   第三条测试 `add_test(NAME golden_ibl_cache_equal COMMAND ${CMAKE_COMMAND} -E
   compare_files ...)`。

- [ ] **Step 5: Commit**

```bash
git add core tests tools
git commit -m "feat(renderer): IBL 预滤波磁盘缓存(内容哈希键;命中跳过渲染)+ set_cache_dir API"
```

---

### Task 3: AGENTS.md 收尾

**Files:**
- Modify: `AGENTS.md`

- [ ] **Step 1: 更新 + 全量回归 + Commit**

AGENTS.md 追加:

```markdown
- IBL 预滤波缓存(F3D 落地):FNV-1a 内容哈希(env 源像素+size+mips);
  `<cache_dir>/ibl/<hash>.ibc`(原子写/坏文件拒绝);默认关,
  `rd_engine_set_cache_dir` 开启;缓存模式预滤波走离屏读回+updateTexture
  (三后端统一);BRDF LUT 为 CPU 纯函数不缓存
```

全量回归 + commit `docs: IBL 缓存收尾(AGENTS.md)`

---

## 附:已知风险与决策记录

| 风险/决策 | 处理 |
|---|---|
| 缓存模式读回成本 | 仅未命中一次性;离屏读回是 GLES 已有路径的推广,三后端已验证 |
| 确定性(同后端) | 同后端同参数逐字节一致(golden_ibl_cache_equal 断言) |
| BRDF LUT | CPU 纯函数(integrateBrdfLut 常量输入),无缓存价值 |
| pipeline 缓存 | v2(VkPipelineCache blob / Metal BinaryArchive,需逐后端) |
| 并发 | 单引擎单线程约定;原子 rename 防半成品 |
